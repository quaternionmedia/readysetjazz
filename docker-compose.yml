version: '3'

networks:
  db:
  web:
    external:
      name: web

services:
  mongo:
    image: mongo
    networks:
      - db
  rsjweb:
    image: rsj
    volumes:
      - './rsj/:/app/'
    labels:
      - 'traefik.HTTP.Routers.RSJ.Rule=Host(`readysetjazz.com`)'
      - 'traefik.docker.network=web'
    networks:
      - db
      - web
    depends_on:
      - mongo
    tty: true
